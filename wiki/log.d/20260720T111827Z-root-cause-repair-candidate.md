# Document the Root Cause Repair local candidate

- Recorded `root-cause-repair/1.0.0` as an unpublished, unlisted local source
  candidate with no canonical manifest, protected evidence, catalog entry, or
  public install path.
- Documented its Git-only high-risk behavior, arbitrary local command and
  uncommitted repository-mutation consequences, install-time agent mappings,
  and `verified`, `not-reproduced`, and `blocked` terminal outcomes.
- Preserved sole repository-owner authority over commits and every remote,
  release, catalog, site, deployment, and Hive-template-removal action.
- Kept checkpoint-only concurrency attribution and provider-backed/public-run
  evidence as explicit gaps, with the entire release tail deferred until an
  owner request.
- Hardened the shared local-state primitive with full double capture, hidden
  index-flag rejection, bounded ignored-path traversal and file growth, and Git
  deadlines; documented that this is not remote-operation attestation.
- Kept the manifest-free source under `candidates/` so committing its tests does
  not turn it into a canonical package submission or bypass contribution rules.
