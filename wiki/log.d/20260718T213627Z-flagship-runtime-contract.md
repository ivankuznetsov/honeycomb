# Validate the agent-agnostic flagship runtime contract

- Required every new executable stage, reviewer, and reviser to declare its
  mapping role, mapping contract revision, and exact permissions while
  rejecting embedded agent, model, or effort identity.
- Added the closed `x-hive` tool and optional-input contract with stable slot
  authorization, package inventory containment, exact executable modes, and
  undeclared-executable rejection.
- Extended security lint's inert behavior scan to exact manifest-declared tool
  paths outside `instructions/`.
- Preserved only three exact historical immutable versions through an
  attributed legacy compatibility allowlist and recorded its retirement gap.
