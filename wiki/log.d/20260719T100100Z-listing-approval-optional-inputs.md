# Normalize optional listing-approval inputs

- Normalized omitted optional dispatch inputs before checking the complete
  protected listing-approval contract.
- Added an explicit empty default for `review_id` and regression coverage for
  both repository-owner and independent dispatch payloads.
