## 2026-07-19 — Pin the Hive v0.6.0 managed-workflow runtime

- Advanced the read-only catalog publication gate to merged Hive v0.6.0 source
  `edaa7131493446f8bb40092fe7801afbaf2f2c04`.
- Made the complete registry contract suite exercise the released runtime that
  supports full workflow payloads, prompt assets, package tools, immutable
  per-slot agent mappings, and scoped optional inputs.
- Kept the dependency immutable and reviewable rather than resolving a moving
  branch or tag in CI.
